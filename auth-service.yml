server:
  port: auth_service_port

spring:
  application:
    name: auth-service

  datasource:
    url: auth_datasource_url
    username: auth_mysql_name
    password: auth_mysql_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: redis_host
      port: redis_port
      password: redis_password

  mail:
    host: smtp.gmail.com
    port: 587
    username: gmail_id
    password: gmail_password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB

naver:
  client-id: naver_id
  client-secret: naver_password
  redirect-uri: naver_redirect

cloud:
  aws:
    credentials:
      access-key: aws_s3_profile_access
      secret-key: aws_s3_profile_secret
    region:
      static: aws_regin_static
    s3:
      bucket: aws_s3_profile_name

mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.example.authservice.model

badword:
  file-path: word_list.json

jwt:
  issuer: jwt_issuer
  secret_key: jwt_secret_key

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.springframework.security.web.FilterChainProxy: DEBUG
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: DEBUG
